import type { FC } from 'react';
import { TabContainer } from './components/TabContainer';

// * --------------------------------------------------------------------------- comp

export const Index: FC = () => {
  return (
    <div>
      <TabContainer />
    </div>
  );
};
